import React from 'react';

export type Props = {
  children: React.ReactNode;
};

export type gad7Result = {
  result: string;
  weight: number;
};

export type asnwerWeight = null | 0 | 1 | 2 | 3;

export type result = {
  title: string;
  desc: string;
  treatment: string;
};
